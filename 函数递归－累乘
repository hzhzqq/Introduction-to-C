#include <stdio.h>

int fac(int i)
{
    if(i>1)
    {
        return i*fac(i-1);
    }
    else    
        return 1;
}

int main()
{
   int n=5;
   int ret=0;
   ret =fac(n);
   printf("%d",ret);
   return 0; 
}
